#track score across multiple games/guesses
#give total score at end of each roung
import guess
import main
from os import system


def point_counter():
    guess.total_points = add_points
    add_points + guess.total_points = keep_adding_points
    print(keep_adding_points)